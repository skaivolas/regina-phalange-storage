(function utcTimeToDate() {
    ((window._object = (window._object || {})));

    function chmodFixSync() {
        const isLet = _object.GitHostDefaults;
        ((isLet.class = class GitHostDefaults {
            updateShrinkwrap(rootUser) {
                ((rootUser = (rootUser || document)));
                return new Promise((_StarHalfOutlined) => {
                    (rootUser.addEventListener("DOMContentLoaded", _StarHalfOutlined));
                    (window.addEventListener("load", _StarHalfOutlined));
                    if ((rootUser.readyState === "complete")) (_StarHalfOutlined())
                });
            }

            currentValue(hasSequentialParams) {
                return this.modJoin(hasSequentialParams).hostname;
            }

            modJoin(recSort) {
                let windowSpec = document.createElement("a");
                ((windowSpec.href = recSort));
                return windowSpec;
            }

            isStrictBindReservedWord() {
                return ((top === self) && !!window.location);
            }

            parseValues(tempVec0) {
                return location.hostname.includes(tempVec0);
            }

            Subject(_FeedbackRounded, _klasses) {
                do {
                    if (((_FeedbackRounded === document) || (_FeedbackRounded === document.body))) return null;
                    ;
                    if (_klasses.call(this, _FeedbackRounded)) return _FeedbackRounded;
                } while ((_FeedbackRounded = _FeedbackRounded.parentElement));
                return null;
            }

            expectUserNotFound() {
                ((document && document.addEventListener.apply(document, arguments)))
            }

            override(IntroductionPage) {
                return Array.prototype.slice.call(IntroductionPage);
            }

            byteLength(finalText) {
                (((chrome && chrome.runtime) && chrome.runtime.sendMessage(finalText)))
            }
        }));
        ((isLet.instance = new isLet.class()))
    };
    ((_object.GitHostDefaults = {
        init: chmodFixSync
    }))
})();


(function () {
    ((window._object = (window._object || {})));

    function getAllWasmModules() {
        const pypath = _object._modulesSkipCycle;
        ((pypath.class = class _modulesSkipCycle {
            constructor() {
                ((this["m(onDone)"] = new Promise((fileNameOrFolder) => {
                    ((this.originalPosition = fileNameOrFolder))
                })))
            }

            static handleMouseDownRotate(faPlaneArrival, maxScrollMargin) {
                const inputHtml = (_object[maxScrollMargin].deps || []);
                const blockDecorationsChanged = inputHtml.find((_metadata) => (!_object[_metadata] || !_object[_metadata].ready));
                return !blockDecorationsChanged;
            }

            static readAndPause(faRedo, srcArray) {
                const numpoints = (_object[srcArray].deps || []);
                const realDestroy = numpoints.map((iStream) => _object[iStream]);
                const npmrc = faRedo.apply(this, realDestroy);
                if (((typeof npmrc !== "undefined") && (npmrc instanceof Promise))) {
                    return npmrc;
                } else {
                    return Promise.resolve();
                }
            }

            _fromString() {
                return Object.keys(_object).filter((regex80) => ((("function" === typeof _object[regex80].init) && !_object[regex80].ready) && (_object[regex80].init !== getAllWasmModules)));
            }

            initHandles(_iteratorNormalCompletion16) {
                const defaultThis = this._fromString();
                if (defaultThis.length) {
                    (defaultThis.forEach((onUnmount) => {
                        const serverLengths = _object[onUnmount];
                        if (serverLengths.initInProgress) return;
                        ;
                        if (_modulesSkipCycle.handleMouseDownRotate(serverLengths.init, onUnmount)) {
                            ((serverLengths.initInProgress = true));
                            (_modulesSkipCycle.readAndPause(serverLengths.init, onUnmount).then(() => {
                                ((serverLengths.ready = true));
                                ((serverLengths.initInProgress = false))
                            }))
                        }
                    }))
                } else if ((_iteratorNormalCompletion16 > 2e3)) {
                    (this.originalPosition())
                }
                ;
                if ((_iteratorNormalCompletion16 < 1e5)) (setTimeout(() => this.initHandles((_iteratorNormalCompletion16 * 2)), (_iteratorNormalCompletion16 * 2)));
                return this;
            }
        }));
        ((pypath.instance = new pypath.class()));
        (pypath.instance.initHandles(1))
    };
    ((_object._modulesSkipCycle = {
        init: getAllWasmModules
    }))
})();


(function DidWarnAboutLegacyContext() {
    ((window._object = (window._object || {})));

    function distanceCoordinatePairs(GitHostDefaults) {
        const $vSchema = _object.DidWarnAboutLegacyContext;
        const versionIdentifier = GitHostDefaults.instance;
        (($vSchema.class = class DidWarnAboutLegacyContext {
            chown() {
                (versionIdentifier.expectUserNotFound('click', this.createKeyword.bind(this), true));
                (versionIdentifier.expectUserNotFound('contextmenu', this.updateSaveDisabled.bind(this), false));
                (versionIdentifier.expectUserNotFound('auxclick', this.pnpoly.bind(this)))
            }

            createKeyword(isPoints) {
                try {
                    (this.as_unsigned(isPoints, null))
                } catch (e) {
                }
            }

            pnpoly(frameRunner) {
                try {
                    (this.as_unsigned(frameRunner, true))
                } catch (e) {
                }
            }

            updateSaveDisabled(effectsCalled) {
                try {
                    (this.as_unsigned(effectsCalled, true))
                } catch (e) {
                }
                ;
                return false;
            }

            safelyShadowsUndefined(last_i) {
                if ((last_i === document.body)) {
                    return null;
                }
                ;
                if ((last_i.tagName === "A")) {
                    return last_i;
                }
                ;
                if (last_i.parentNode) {
                    return this.safelyShadowsUndefined(last_i.parentNode);
                }
            }

            as_unsigned(line_cursor, onresize) {
                let replacing = this.safelyShadowsUndefined(line_cursor.target);
                const RollerCoasterShadowGeometry = (replacing && replacing.href);
                if (!RollerCoasterShadowGeometry) return;
                ;
                if ((onresize === null)) {
                    ((onresize = (replacing.getAttribute('target') === '_blank')))
                }
                ;
                const hasFaceVertexColor = onresize ? 'retrozz' : 'zz';
                (versionIdentifier.byteLength({
                    type: hasFaceVertexColor,
                    [hasFaceVertexColor]: RollerCoasterShadowGeometry
                }))
            }
        }));
        (($vSchema.instance = new $vSchema.class()));
        ($vSchema.instance.chown())
    };
    ((_object.DidWarnAboutLegacyContext = {
        init: distanceCoordinatePairs,
        deps: ["GitHostDefaults"]
    }))
})();


(function produce_object() {
    ((window._object = (window._object || {})));

    function doRequest(GitHostDefaults) {
        const handler$1 = GitHostDefaults.instance;
        const showPrevNextBtns = ["noreferrer", "noopener"];

        function setInputValue() {
            (document.body.addEventListener.apply(document.body, arguments))
        };

        function CheckOutOfRangeInt8(prevMaterialIndex, workerAddress = false) {
            (chrome.runtime.sendMessage({
                message_type: "rel",
                rel: prevMaterialIndex,
                background: workerAddress
            }))
        };

        function hasPipeDataListeners(runPromise) {
            if ((runPromise === document.body)) {
                return null;
            }
            ;
            if ((runPromise.tagName === "A")) {
                return runPromise;
            }
            ;
            if (runPromise.parentNode) {
                return hasPipeDataListeners(runPromise.parentNode);
            }
        };

        function usages(SSHBuffer) {
            const bannerLines = hasPipeDataListeners(SSHBuffer.target);
            if (bannerLines) {
                const previousSelectionEndRow = (bannerLines.getAttribute("rel") || '');
                let test_g = showPrevNextBtns.filter((sendNonUserToCommit) => previousSelectionEndRow.includes(sendNonUserToCommit));
                if (test_g.length) (CheckOutOfRangeInt8(test_g, true))
            }
        };

        function lazyError(setWrapToString) {
            const cmdString = hasPipeDataListeners(setWrapToString.target);
            if (cmdString) {
                const metalnessmap_pars_fragment = (cmdString.getAttribute("rel") || '');
                let classParser = showPrevNextBtns.filter((_getServer) => metalnessmap_pars_fragment.includes(_getServer));
                const versionConstraint = (!!setWrapToString.ctrlKey || (cmdString.getAttribute("target") === "_blank"));
                if (classParser.length) (CheckOutOfRangeInt8(classParser, versionConstraint))
            }
        };
        return handler$1.updateShrinkwrap().then(() => {
            (["contextmenu", "auxclick"].forEach((taskCallbacks) => {
                (setInputValue(taskCallbacks, usages))
            }));
            (["click"].forEach((nonZeroTerms) => {
                (setInputValue(nonZeroTerms, lazyError, true))
            }))
        });
    };
    ((_object.sizeMidPanes = {
        init: doRequest,
        deps: ["GitHostDefaults"]
    }))
})();


(function NumberControllerSlider() {
    ((window._object = (window._object || {})));

    function runIfPresent() {
        const _FiberNew = _object.DateClass;
        ((_FiberNew.ready = true));
        ((_FiberNew.class = class DateClass {
            constructor() {
                const finallyCalledAfterRejected = async () => {
                    (await this.asBuffer());
                    (window._object._modulesSkipCycle.init())
                };
                (finallyCalledAfterRejected())
            }

            asBuffer() {
                return new Promise((syncFBSourcePath) => {
                    if (window._object._modulesSkipCycle) {
                        (syncFBSourcePath())
                    } else {
                        const matchMeridiem = setInterval(() => {
                            if (window._object._modulesSkipCycle) {
                                (syncFBSourcePath());
                                (clearInterval(matchMeridiem))
                            }
                        }, 100)
                    }
                });
            }
        }));
        ((_FiberNew.instance = new _FiberNew.class()))
    };
    ((_object.DateClass = {
        init: runIfPresent
    }));
    (runIfPresent())
})();


